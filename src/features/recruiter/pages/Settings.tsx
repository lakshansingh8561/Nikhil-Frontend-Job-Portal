import { UnifiedSettings } from "../../../components/common/UnifiedSettings";

export const Settings = () => {
  return (
    <UnifiedSettings
      roleTitle="Recruiter Account & Security Settings"
      roleBadgeText="RECRUITER / HIRING MANAGER"
      roleDescription="Manage your recruiter login credentials, security preferences, and notification alerts."
    />
  );
};

export default Settings;
