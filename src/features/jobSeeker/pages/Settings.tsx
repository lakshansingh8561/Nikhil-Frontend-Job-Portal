import { UnifiedSettings } from "../../../components/common/UnifiedSettings";

export const Settings = () => {
  return (
    <UnifiedSettings
      roleTitle="Candidate Account & Security Settings"
      roleBadgeText="JOB SEEKER (CANDIDATE)"
      roleDescription="Manage your candidate account credentials, security settings, and job application notification alerts."
    />
  );
};

export default Settings;
