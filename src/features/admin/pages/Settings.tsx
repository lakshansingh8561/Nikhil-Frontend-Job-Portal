import { UnifiedSettings } from "../../../components/common/UnifiedSettings";

export const Settings = () => {
  return (
    <UnifiedSettings
      roleTitle="Administrator Settings & Control Panel"
      roleBadgeText="SYSTEM SUPER ADMIN"
      roleDescription="Manage super admin security credentials, global system preferences, and session controls."
    />
  );
};

export default Settings;
